# =============================================================================
# TreakHigh — Nginx Server Block
# =============================================================================
# ARCHITECTURE:
#   - Serves static frontend: public/ files at root, src/ for JS/CSS modules
#   - Proxies /api/telemetry to n8n webhook with server-side HMAC signing
#   - Enforces security headers, CORS (origin-validated), and rate limiting
#   - The njs module (hmac-signer.js) signs payloads before proxying,
#     so the HMAC_SECRET never reaches the browser
# =============================================================================

# ── CORS Origin Whitelist ────────────────────────────────────────────────────
# Only these origins may call /api/telemetry.
# Add production domains here; empty string = reject unknown origins.
map $http_origin $cors_origin {
    default              "";
    "http://localhost"   $http_origin;
    "http://localhost:80" $http_origin;
    "https://treakhigh.app" $http_origin;
}

server {
    listen 80;
    server_name localhost;

    # ── Security Headers ─────────────────────────────────────────────────
    add_header X-Content-Type-Options    "nosniff" always;
    add_header X-Frame-Options           "DENY" always;
    add_header X-XSS-Protection          "1; mode=block" always;
    add_header Referrer-Policy           "strict-origin-when-cross-origin" always;
    add_header Content-Security-Policy   "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; connect-src 'self';" always;

    # ── Static Frontend (public/) ────────────────────────────────────────
    # The Docker volume mounts ./frontend → /usr/share/nginx/html
    # index.html lives in public/, so root points there.
    location / {
        root /usr/share/nginx/html/public;
        index index.html;
        try_files $uri $uri/ =404;

        # Cache static assets aggressively
        location ~* \.(png|jpg|jpeg|gif|ico|svg|woff2?)$ {
            expires 7d;
            add_header Cache-Control "public, immutable";
        }
    }

    # ── JS/CSS Modules (src/) ────────────────────────────────────────────
    # Serves ES module imports from /src/ — maps to frontend/src/ on disk.
    location /src/ {
        alias /usr/share/nginx/html/src/;
        try_files $uri =404;

        # Cache JS/CSS with moderate expiry (they may change during dev)
        location ~* \.(js|css)$ {
            expires 1d;
            add_header Cache-Control "public";
        }
    }

    # ── Telemetry API Proxy ──────────────────────────────────────────────
    # proxy to go webhook (internal network — ingester on frontend_net)
    # The Golang service now handles HMAC compute natively as requested
    # by the migration plan.
    # ─────────────────────────────────────────────────────────────────────
    location /api/telemetry {
        # Rate limiting: 10 req/s per IP, burst 20
        limit_req zone=telemetry_limit burst=20 nodelay;
        limit_req_status 429;

        # CORS headers — only whitelisted origins (see map above)
        add_header Access-Control-Allow-Origin  "$cors_origin" always;
        add_header Access-Control-Allow-Methods "POST, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Content-Type" always;
        add_header Access-Control-Max-Age       "86400" always;

        # Handle CORS preflight
        if ($request_method = OPTIONS) {
            return 204;
        }

        # Only allow POST
        limit_except POST {
            deny all;
        }

        client_body_buffer_size 128k;
        client_max_body_size 1m;

        # Proxy to the Golang Ingester service
        proxy_pass http://ingester:8080/api/telemetry;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Content-Type "application/json";

        # Timeouts for the upstream
        proxy_connect_timeout 5s;
        proxy_read_timeout    30s;
        proxy_send_timeout    10s;
    }

    # ── Quiz Bundles API Proxy ─────────────────────────────────────────────
    # INFRASTRUCTURE FIX: Route /api/bundles/ to n8n so the frontend can
    # fetch real quiz JSON from the backend workflow engine.
    # Example: GET /api/bundles/math-101 → n8n webhook/quiz-bundles/math-101
    location /api/bundles/ {
        proxy_pass http://n8n-main:5678/webhook/quiz-bundles/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeouts for the upstream
        proxy_connect_timeout 5s;
        proxy_read_timeout    30s;
        proxy_send_timeout    10s;
    }

    # ── Health Check Endpoint ────────────────────────────────────────────
    location /health {
        access_log off;
        return 200 '{"status":"ok"}';
        add_header Content-Type application/json;
    }

    # ── Block dotfiles ───────────────────────────────────────────────────
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
}
